# Secrets — never commit
.env

# Environments
.venv/
__pycache__/
*.egg-info/

# Generated corpora (rebuild with scripts/build_corpora.py)
corpora/*.log
corpora/*.jsonl

# Built pages under results/ are scratch; the docs/ copies are published.
results/index.html
results/slides.html
*.png

# Run logs are scratch; the JSON measurements they produced are the evidence.
results/*.log
results/*.txt

# Measurements ARE the evidence — results/*.json is committed deliberately.
