[project]
name = "packitless"
version = "0.1.0"
description = "Budget-aware context compression for repetitive LLM payloads."
requires-python = ">=3.11"
dependencies = [
    "drain3>=0.9.11",
    "anthropic>=0.40.0",
]

[project.scripts]
packitless = "packitless.cli:main"
packitless-mcp = "packitless.integrations.mcp_server:main"

[project.optional-dependencies]
mcp = ["mcp>=1.0"]
plot = ["matplotlib>=3.8"]
dev = ["pytest>=8.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["packitless"]
